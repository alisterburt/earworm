{
  "name": "chops-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:pages": "CHOPS_BASE=/chops/ vite build",
    "preview": "vite preview",
    "deploy": "npm run build:pages && gh-pages -d dist --dotfiles"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "gh-pages": "^6.3.0",
    "svelte": "^5.0.0",
    "vite": "^6.0.0"
  }
}
