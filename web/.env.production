# Public base URL of the R2 bucket (its r2.dev dev URL or a custom domain).
# When set, production builds fetch all song content from here and leave
# public/content out of dist. Leave empty to keep bundling content locally.
# To keep the URL out of the repo, set it in .env.production.local (gitignored).
VITE_CONTENT_BASE=
